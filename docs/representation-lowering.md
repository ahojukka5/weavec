# Representation lowering

The former mixed implementation/proposal document has been superseded.

Current compiler layering and the surface-to-executable pipeline are documented
in [Architecture](architecture.md). Implemented quantum lowering and future
runtime boundaries are documented in [Quantum surface support](quantum.md).

This path remains only for historical links. It does not define an additional IR,
macro system, transform registry, or public syntax contract.
