; Consecutive Y on the same qubit cancel (Y is self-inverse, Y^2 = I).
(program
  (name "test-double-y-cancel")
  (version "0.1")
  (entry main
    (params)
    (returns i32)
    (do
      (let q0 Qubit (const_i64 0))
      (qgate Y q0)
      (qgate Y q0)
      (return (const_i32 42)))))
