; Consecutive Z on the same qubit cancel (Z is self-inverse, Z^2 = I).
(program
  (name "test-double-z-cancel")
  (version "0.1")
  (entry main
    (params)
    (returns i32)
    (do
      (let q0 Qubit (const_i64 0))
      (qgate Z q0)
      (qgate Z q0)
      (return (const_i32 42)))))
