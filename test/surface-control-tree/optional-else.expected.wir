(core-module
  (core-version 3)
  (decls
    (fn main (params) (returns i32) (do (let n i32 (const_i32 1)) (if (condition (const_bool false)) (then (do (set n (const_i32 0)))) (else (do))) (return n)))
  )
)
