(program
  (name "audit_one_if_return")
  (version "0.1")
  (entry main)
  (fn f
    (params (x i32))
    (returns i32)
    (do
      (if
        (condition (lt_i32 x (const_i32 0)))
        (then (do (return (const_i32 0))))
        (else (do)))
      (return x))))
