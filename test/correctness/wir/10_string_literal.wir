(core-module
  (core-version 3)
  (decls
    (fn main
      (params)
      (returns i32)
      (do
        (print
          (const_string "hello from weave")
        ) ;; print
        (return
          (const_i32 42)
        ) ;; return
      ) ;; do
    ) ;; fn
  ) ;; decls
) ;; core-module
