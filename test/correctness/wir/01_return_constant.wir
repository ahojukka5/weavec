(core-module
  (core-version 3)
  (decls
    (fn main
      (params)
      (returns i32)
      (do
        (return
          (const_i32 0)
        ) ;; return
      ) ;; do
    ) ;; fn
  ) ;; decls
) ;; core-module
